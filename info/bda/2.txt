Problem Statement

Implement Matrix Multiplication using MapReduce.

The program should:

Multiply two matrices
Use Mapper and Reducer concepts
Generate final resultant matrix
🔷 First Understand Matrix Multiplication

Suppose:

Matrix A
A=[
1
3
	​

2
4
	​

]
Matrix B
B=[
2
4
	​

1
3
	​

]

We want:

C=A×B
🔷 Matrix Multiplication Formula

For each element:

C[i][j]=∑
k=0
n−1
	​

A[i][k]×B[k][j]

Meaning:

Take row from A
Take column from B
Multiply corresponding elements
Add them
🔷 Manual Calculation
🔶 C[0][0]
(1×2)+(2×4)
2+8=10
🔶 C[0][1]
(1×1)+(2×3)
1+6=7
🔶 C[1][0]
(3×2)+(4×4)
6+16=22
🔶 C[1][1]
(3×1)+(4×3)
3+12=15
🔷 Final Matrix
C=[
10
22
	​

7
15
	​

]
🔷 Why Use MapReduce for Matrix Multiplication?

In Big Data:

matrices can be huge
millions of rows/columns

MapReduce helps:

split work
process parallelly
improve speed
🔷 MapReduce Concept Here
🔶 Mapper Phase

Mapper:

generates intermediate key-value pairs

Key:

(i, j)

Value:

(matrix_name, k, value)

Example:

((0,0), ('A',0,1))

Meaning:

for result position (0,0)
A contributes value 1
🔶 Reducer Phase

Reducer:

groups same keys
performs multiplication
adds results
🔷 Full Code
# Mapper function
def mapper(A, B):
    mapped = []

    # A: (i, k, value)
    for i in range(len(A)):
        for k in range(len(A[0])):
            for j in range(len(B[0])):
                mapped.append(((i, j), ('A', k, A[i][k])))

    # B: (k, j, value)
    for k in range(len(B)):
        for j in range(len(B[0])):
            for i in range(len(A)):
                mapped.append(((i, j), ('B', k, B[k][j])))

    return mapped


# Reducer function
def reducer(mapped_data):
    from collections import defaultdict

    grouped = defaultdict(list)

    # Group by key (i, j)
    for key, value in mapped_data:
        grouped[key].append(value)

    result = {}

    # Compute multiplication
    for key, values in grouped.items():
        a_vals = {}
        b_vals = {}

        for tag, k, val in values:
            if tag == 'A':
                a_vals[k] = val
            else:
                b_vals[k] = val

        total = 0
        for k in a_vals:
            if k in b_vals:
                total += a_vals[k] * b_vals[k]

        result[key] = total

    return result


# Driver code
def main():
    A = [
        [1, 2],
        [3, 4]
    ]

    B = [
        [2, 1],
        [4, 3]
    ]

    mapped = mapper(A, B)
    result = reducer(mapped)

    print("Result Matrix:")
    for (i, j), value in sorted(result.items()):
        print(f"C[{i}][{j}] = {value}")


if __name__ == "__main__":
    main()
🔷 Line-by-Line Explanation
🔶 Mapper Function
def mapper(A, B):
Explanation:

Mapper takes:

Matrix A
Matrix B

and generates intermediate key-value pairs.

🔶 Empty List
mapped = []

Stores mapper output.

🔷 Processing Matrix A
for i in range(len(A)):

Loops through rows of A.

for k in range(len(A[0])):

Loops through columns of A.

for j in range(len(B[0])):

Loops through columns of B.

🔶 Mapper Output for A
mapped.append(((i, j), ('A', k, A[i][k])))
Explanation:

Creates:

((i,j), ('A',k,value))

Example:

((0,0), ('A',0,1))

Meaning:

result cell (0,0)
receives contribution from matrix A
k = 0
value = 1
🔷 Processing Matrix B
for k in range(len(B)):

Loops rows of B.

for j in range(len(B[0])):

Loops columns of B.

for i in range(len(A)):

Loops rows of A.

🔶 Mapper Output for B
mapped.append(((i, j), ('B', k, B[k][j])))
Explanation:

Creates:

((i,j), ('B',k,value))

Example:

((0,0), ('B',0,2))
🔶 Return Mapper Data
return mapped

Returns all key-value pairs.

🔷 Reducer Function
def reducer(mapped_data):

Reducer processes mapper output.

🔶 Import defaultdict
from collections import defaultdict

Creates dictionary with default values.

🔶 Grouped Dictionary
grouped = defaultdict(list)

Stores grouped values.

🔶 Group by Key
for key, value in mapped_data:
    grouped[key].append(value)
Explanation:

Groups all values for same (i,j).

Example:

(0,0) → [('A',0,1), ('B',0,2)]
🔶 Result Dictionary
result = {}

Stores final matrix.

🔷 Process Each Position
for key, values in grouped.items():

Processes each matrix cell.

🔶 Separate A and B Values
a_vals = {}
b_vals = {}

Stores:

A values
B values

separately.

🔶 Read Tagged Values
for tag, k, val in values:

Reads:

matrix name
index k
value
🔶 Store A Values
if tag == 'A':
    a_vals[k] = val

Example:

a_vals[0] = 1
🔶 Store B Values
else:
    b_vals[k] = val

Example:

b_vals[0] = 2
🔶 Initialize Total
total = 0

Stores final sum.

🔶 Multiply and Add
for k in a_vals:

Loops through common k values.

if k in b_vals:

Checks matching k.

total += a_vals[k] * b_vals[k]
Explanation:

Performs:
total=total+A[i][k]×B[k][j]

🔶 Store Final Value
result[key] = total

Stores result matrix element.

Example:

result[(0,0)] = 10
🔶 Return Result
return result

Returns final matrix.

🔷 Driver Code
def main():

Program starts here.

🔶 Define Matrix A
A = [
    [1, 2],
    [3, 4]
]
🔶 Define Matrix B
B = [
    [2, 1],
    [4, 3]
]
🔶 Call Mapper
mapped = mapper(A, B)

Runs mapper phase.

🔶 Call Reducer
result = reducer(mapped)

Runs reducer phase.

🔶 Print Output
for (i, j), value in sorted(result.items()):

Sorts matrix positions.

print(f"C[{i}][{j}] = {value}")

Prints matrix values.

🔷 Final Output
Result Matrix:
C[0][0] = 10
C[0][1] = 7
C[1][0] = 22
C[1][1] = 15
🔷 Important Terms
Term	Meaning
Matrix	2D array of numbers
MapReduce	Distributed processing model
Mapper	Produces intermediate key-value pairs
Reducer	Combines and calculates results
Key	Matrix position (i,j)
Value	Matrix data contribution
🔷 Advantages of MapReduce Matrix Multiplication
Advantage	Explanation
Parallel Processing	Faster multiplication
Scalable	Works on huge matrices
Distributed	Uses multiple machines
Efficient	Suitable for Big Data
🔷 Real Life Applications

Used in:

Machine Learning
Image Processing
Scientific Computing
AI systems
Recommendation systems
Graphics processing